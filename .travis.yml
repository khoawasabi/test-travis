language: php

php:
  - '5.6'
  - hhvm
  - nightly

before_script:
  - composer install --dev --no-interaction

script:
  - mkdir -p build/logs
  - vendor/bin/phpunit --bootstrap src/Sum.php tests/
after_script: ./codeclimate.sh